package com.example.firstspringbootapp.demo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DemoApplicationTests {

	@Test
	void contextLoads() {
		// Intentionally empty: this test's only purpose is to verify that
		// the Spring application context starts up without throwing any
		// exceptions. No assertions are needed — a failed context load
		// will cause this test to fail automatically.
	}

}
